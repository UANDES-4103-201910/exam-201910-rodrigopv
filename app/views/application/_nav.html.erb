  <nav class="my-2 my-md-0 mr-md-3">
    <a class="p-2 text-dark" href="#">Handsets</a>
    <a class="p-2 text-dark" href="#">Prepay Options</a>
    <a class="p-2 text-dark" href="#">Subscriptions</a>
  </nav>
  <% if user_signed_in? %>
    <% if ! session[:cart].nil? %>
     <%= link_to(shopping_carts_url) do %> 
       <span class="mr-1 btn btn-outline-secondary">Cart: <%= session[:cart].count %> items. </span>
     <% end %>
    <% end %>
  <% end %>
